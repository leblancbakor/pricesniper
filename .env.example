# Copy this file to `.env` and fill in real values. `.env` is gitignored.
# Nothing here is used yet at v0.1 — these are placeholders for upcoming stages.

# Discord bot token (v0.3 — alerting)
DISCORD_BOT_TOKEN=

# Database URL (v0.3 — starts as SQLite, e.g. sqlite:///data/pricesniper.db)
DATABASE_URL=sqlite:///data/pricesniper.db
